- name: Set up Python
  uses: actions/setup-python@v4
  with:
    python-version: '3.10'

- name: Install Dependencies
  run: |
    python -m pip install --upgrade pip
    pip install geopandas shapely pandas requests

- name: Run Port Risk Processing
  run: |
    python scripts/process_port_risks.py
